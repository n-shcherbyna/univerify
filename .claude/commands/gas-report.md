Run a gas report for the smart contracts.

1. Run `cd /home/nazar/projects/univerify/contracts && forge test --gas-report`
2. Summarize the results in a table showing function name, min/avg/median/max gas costs.
3. Highlight the most expensive functions and suggest optimizations if any are obvious.

$ARGUMENTS
